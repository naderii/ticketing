@model IEnumerable<TicketingSystem.Models.Ticket>
@using System.Security.Claims


@{
    ViewData["Title"] = "لیست تیکت‌ها";
}
@foreach (var ticket in Model)
{
@*     <div>
        @ticket.Title
        @if (User.IsInRole("Admin") || User.IsInRole("Support") || ticket.UserId == User.FindFirstValue(ClaimTypes.NameIdentifier))
        {
            <p>@ticket.Description</p>
        }
    </div> *@
}



<div class="container mt-5">
    <h2 class="text-center mb-4">لیست تیکت‌ها</h2>

    <!-- دکمه ایجاد تیکت جدید -->
    <div class="d-flex justify-content-between mb-4">
        <a href="@Url.Action("Create", "Tickets")" class="btn btn-success">ایجاد تیکت جدید</a>
    </div>

    <!-- جدول نمایش تیکت‌ها -->
    <div class="card shadow-lg">
        <div class="card-body">
            <table class="table table-striped table-hover">
                <thead class="thead-dark">
                    <tr>
                        <th>شماره</th>
                        <th>عنوان</th>
                        <th>اولویت</th>
                        <th>تاریخ ایجاد</th>
                        <th>عملیات</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var ticket in Model)
                    {
                        <tr>
                            <td>@ticket.Id</td>
                            <td>@ticket.Title</td>
                            <td>
                                @if (ticket.Priority == "High")
                                {
                                    <span class="badge bg-danger">بالا</span>
                                }
                                else if (ticket.Priority == "Medium")
                                {
                                    <span class="badge bg-warning">متوسط</span>
                                }
                                else
                                {
                                    <span class="badge bg-success">پایین</span>
                                }
                            </td>
                            <td>@ticket.CreatedAt.ToShortDateString()</td>
                            <td>
                                <a href="@Url.Action("Details", "Tickets", new { id = ticket.Id })" class="btn btn-primary btn-sm">جزئیات</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        // این کد برای اعمال رنگ‌بندی (badge) به اولویت‌ها است.
    </script>
}
